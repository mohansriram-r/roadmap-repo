import 'package:flutter/material.dart';

class StratScreen extends StatelessWidget {
  const StratScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("StartScreen");
  }
}
